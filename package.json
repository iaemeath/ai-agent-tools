{
  "name": "ccc-ui",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "description": "Manage AI coding-agent config across tools (Claude Code, ZCode) — skills, plugins, projects, instructions, rules, commands, agents, hooks, MCP, settings (Config-as-SSOT). Node + Hono + Vue 3 + Element Plus.",
  "workspaces": [
    "server",
    "web"
  ],
  "scripts": {
    "dev:server": "tsx watch server/src/index.ts",
    "dev:web": "npm -w web run dev",
    "dev": "concurrently -k -n server,web -c blue,green \"npm:dev:server\" \"npm:dev:web\"",
    "build": "npm -w web run build",
    "build:exe": "node scripts/build-exe.mjs",
    "start": "tsx server/src/index.ts",
    "typecheck": "npm -w server exec tsc && npm -w web exec vue-tsc"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "postject": "1.0.0-alpha.6",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
